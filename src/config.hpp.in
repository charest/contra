#ifndef CONFIG_HPP
#define CONFIG_HPP

#include <cstdint>
#include <string>

#cmakedefine HAVE_LEGION
#define REALM_LIBRARY "@REALM_LIBRARY@"
#define LEGION_LIBRARY "@Legion_LIBRARY@"

#cmakedefine HAVE_KOKKOS
#cmakedefine HAVE_CUDA

namespace {

  using int_t = std::int64_t;
  using uint_t = std::uint64_t;
  using real_t = double;
  using byte_t = unsigned char;

  constexpr char TargetNvptxString[] = "nvptx64";

  constexpr size_t SmallVectorLength = 10240;

} // namespace

#endif // CONFIG_HPP
