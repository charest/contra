#ifndef CONFIG_HPP
#define CONFIG_HPP

#include <cstdint>
#include <string>

#cmakedefine HAVE_LEGION
#define REALM_LIBRARY "@REALM_LIBRARY@"
#define LEGION_LIBRARY "@Legion_LIBRARY@"

#cmakedefine HAVE_KOKKOS
#cmakedefine HAVE_CUDA

#ifdef HAVE_CUDA
#include "config.defs"
#endif

#cmakedefine HAVE_ROCM
#define ROCM_LD_LLD_PATH "@ROCM_LD_LLD_EXE@"
#define ROCM_LLVM_LINK_PATH "@ROCM_LLVM_LINK_EXE@"

#ifdef __CUDACC__
#define CONTRA_INLINE_TARGET __host__ __device__
#else
#define CONTRA_INLINE_TARGET
#endif

namespace {

  using int_t = std::int64_t;
  using uint_t = std::uint64_t;
  using real_t = double;
  using byte_t = unsigned char;

  constexpr size_t SmallVectorLength = 10240;

} // namespace

#endif // CONFIG_HPP
